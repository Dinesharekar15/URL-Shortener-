<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Url Shortner</title>

    <style>
        body {
            background: #263849;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        h1 {
            color: #fff;
            font-size: 46px;
            font-weight: 500;
            letter-spacing: -.1px;
            line-height: 54px;
            margin-bottom: 16px;
        }
        .contain{
            width: 100%;
            display: flex;
            justify-content: center;
        }

        input {
            background-color: #fff;
            border: 1px solid rgba(175, 185, 197, .36);
            border-radius: 4px;
            color: #161c34;
            font-size: 20px;
            height: 30px;
            line-height: 28px;
            margin-bottom: 0;
            padding-bottom: 11px;
            padding-top: 11px;
            
        }

        button {
            background-color: #2281c2;
            border-radius: 4px;
            color: #fff;
            font-size: 20px;
            font-weight: 500;
            line-height: 50px;
        }

        .all {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
            width: 100%;
        }
    </style>


</head>

<body>
    <div class="all">
        <h1> Free URL Shortener</h1>
        <div class="shortid">
            <% if (locals.id) { %>

                <p>
                <h3>Generated Id:</h3>http://localhost:7000/<%= id %>
                    </p>
                    <% } %>
        </div>
        <div class="contain">
            <form method="POST" action="/url">

                
                    <input type="text" name="url" placeholder="Enter yor link here">
                    <button type="submit">Shorten url</button>
                

            </form>
        </div>
        <div class="table">
            <h2>URL's</h2>
            <% if (locals.urls) { %>
                <table>
                    <thead>
                        <tr>
                            <th>sr.no</th>
                            <th>short url</th>
                            <th>redirect</th>
                            <th>clicks</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% urls.forEach((url, index)=> { %>
                            <tr>
                                <td>
                                    <%= index + 1 %>
                                </td>
                                <td>
                                    <%= url.shorturlid %>
                                </td>
                                <td>
                                    <%= url.redirecturl %>
                                </td>
                                <td>
                                    <%= url.visithistory.length %>
                                </td>
                            </tr>
                            <% }); %>
                    </tbody>
                </table>
                <% } %>
        </div>

    </div>
</body>

</html>


</html>